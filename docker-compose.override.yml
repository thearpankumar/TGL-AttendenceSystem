services:
  
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    deploy:
      replicas: 1
    environment:
      - NODE_ENV=development
      - MONGODB_URI=mongodb://mongo1:27017/attendance-geotag
      - REDIS_URL=redis://redis:6379
    volumes:
      - ./backend/src:/app/src:ro

  
  mongodb-init:
    entrypoint: ["mongosh", "--host", "mongo1", "--port", "27017", "/init-mongo.js"]
