# Student Frontend Configuration
# Static site - API URL determined at runtime from window.location.origin
# No build-time configuration needed

# Storage behavior is determined by backend's STORAGE_PROVIDER setting
